# Instructions:

    composer install

    cp .env.example .env

    npm install

    php artisan migrate --seed

    php artisan serve

    npm run dev

**Note:**
    The application be access on the root URL.
